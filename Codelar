#region Task1
//.Verilmiş Arrayın elementlərinin cəmini ekrana çap edin
int[] numbers = { 10, 12, 13 };
int sum = 0;
for(int i=0; i<numbers.Length;i++)
{
    sum += numbers[i];
}
Console.WriteLine(sum);
#endregion

#region Task2 
//Verilmiş int Arrayın ən böyük dəyərə sahib elementini tapın
int[] ededler = { 1, 2, 3, 15 };
int enboyuk = ededler[0];
int enkicik = ededler[0];
foreach (int eded in ededler)
{
    if (eded > enboyuk)
    {
        enboyuk = eded;
    }
}
Console.WriteLine("Enboyuk" + enboyuk);
#endregion 

#region Task3
//Verilmiş Arrayın ilk və son elementlərinin cəmini tapın
int[] num = { 10, 20, 30, 40, 50 };
int ilkeded  = num[0];
int soneded  = num[^1];
Console.WriteLine(ilkeded + soneded);
#endregion

#region Task4 
// Verilmiş müsbət tam n ədədinin 2-nin qüvvəti olub-olmamasını tapın
int numm = 64;
while(numm%2==0&&numm>1)
{
    numm /= 2;
}
if (numm == 1)
{
    Console.WriteLine("2nin quvvetidir");
}
else
{
    Console.WriteLine("2nin quvveti deyil");
}
#endregion

#region Task5
//Verilmiş n tam ədədinin neçə mərtəbəli olduğunu tapın. Məs: 23452, output: 5   
int number = 23452;
int count = 0;
while(number>0)
{
    number /= 10;
    count++;
}
if(count==0)
{
    Console.WriteLine(1);
}
else
{
    Console.WriteLine(count);
}
#endregion

#region Task6
//Tam ədədlər ardıcıllığı verilir.
//Ardıcıllığın hər bir mənfi olmayan elementini 2 vahid artırmalı.
int[] nums = { -2, -1, 0, 1, 2, 3 };
for (int j = 0; j < nums.Length; j++)
{
    if (nums[j] >= 0)
    {
        nums[j] += 2;
    }
    Console.WriteLine(nums[j]);
}
#endregion

#region Task7
//Verilmiş massivin ən kiçik elementini çıxışa verən proqram yazın
int[] bbc = { 1, 2, 3, 4, 5, 6 };
int enkicikk = bbc[0];
foreach(int numms in bbc)
{
    if(numms< enkicik)
    {
        enkicik = numms;
    }
}
Console.WriteLine("Enkicik" + enkicik);
#endregion
